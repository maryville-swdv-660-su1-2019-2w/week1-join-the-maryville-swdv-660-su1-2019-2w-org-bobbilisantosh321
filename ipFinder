import requests
from colorama import init, Fore

init()

response = requests.get('https://httpbin.org/ip')

print(Fore.GREEN + 'Your IP is {0}'.format(response.json()['origin']))

input("\n Press ENTER to continue")